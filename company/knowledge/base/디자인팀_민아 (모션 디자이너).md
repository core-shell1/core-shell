
### GSAP 3.12.5 ScrollTrigger advanced patterns: pinning, scrub, batch, timeline synchronization with Lenis smooth scroll
### **GSAP 3.12.5 ScrollTrigger 고급 패턴: Pinning, Scrub, Batch, Timeline + Lenis 동기화**

GSAP 3.12.5에서 **ScrollTrigger**는 스크롤 기반 애니메이션의 핵심. Pinning(고정), Scrub(스크롤 연동), Batch(배치 처리), Timeline 동기화로 복잡한 스크롤 시퀀스 구현. Lenis(스무스 스크롤)와 결합 시 `scrollerProxy`로 완벽 동기화[1][2][3].

#### **1. Pinning: 요소 고정 (Advanced Lock-in)**
- **핵심**: `pin: true`로 요소를 스크롤 중 고정. 자동 `pinSpacing`으로 후속 요소 밀어냄 (비활성화: `pinSpacing: false`).
- **실전 코드** (500px 동안 고정 + 애니메이션):
```javascript
let tl = gsap.timeline({
  scrollTrigger: {
    trigger: '.container',
    pin: true,          // 고정 시작
    start: 'top top',   // 뷰포트 top 도달 시
    end: '+=500',       // 500px 스크롤 후 해제
    scrub: 1            // 1초 지연 스무딩
  }
});
tl.from('.box p', { scale: 0.3, rotation: 45, autoAlpha: 0 })
  .to('.box', { rotation: 360 });
```
- **팁**: 다중 pinning 가능 (같은 요소 여러 지점 고정). 3.12+ `clamp("top bottom")`으로 오버스크롤 방지[1][2].
- **성능**: `anticipatePin: 1`로 미리 padding 계산 (긴 페이지 필수)[1].

#### **2. Scrub: 스크롤 1:1 연동 (Smooth Catch-up)**
- **핵심**: `scrub: true` (즉시), `scrub: 1` (1초 catch-up), `scrub: {start: 0.5, end: 2}` (가변 스무딩).
- **실전 코드** (부드러운 x 이동):
```javascript
gsap.to('.box', {
  x: 500,
  scrollTrigger: {
    trigger: '.box',
    start: 'top center',
    end: 'bottom top',
    scrub: 1.5  // 1.5초 catch-up (자연스러움)
  }
});
```
- **고급**: `getVelocity()`로 스크롤 속도 기반 snap. `snap: 0.1` (10% 단위) 또는 `snap: [0, 0.25, 0.5, 0.75, 1]`[1][3].
- **3.12 이슈 피하기**: 페이지 리프레시 시 `ScrollTrigger.refresh()` 호출 (scrollTop != 0일 때 브레이크)[5].

#### **3. Batch: 다중 요소 배치 처리 (성능 최적화)**
- **핵심**: `ScrollTrigger.batch()`로 10+ 요소 스크롤 트리거 묶음. RAF(60fps) 단일화로 CPU 80% 절감.
- **실전 코드** (뷰포트 진입 시 stagger 페이드인):
```javascript
ScrollTrigger.batch('.fade-item', {
  onEnter: batch => gsap.from(batch, { opacity: 0, y: 50, stagger: 0.1 }),
  onEnterBack: batch => gsap.from(batch, { opacity: 0, y: 50, stagger: 0.1 }),
  onLeave: batch => gsap.to(batch, { opacity: 0, y: -50, stagger: 0.1 }),
  start: 'top 80%',
  end: 'bottom 20%',
  markers: true  // 디버그용
});
```
- **팁**: 50+ 이미지/텍스트에 필수. `matchMedia`로 반응형 batch (모바일/데스크톱 분리)[3].

#### **4. Timeline 동기화: 복합 시퀀스 (Choreographed Scroll)**
- **핵심**: Timeline 전체에 ScrollTrigger 부여. Label snap으로 섹션 점프.
- **실전 코드** (Pin + Multi-step):
```javascript
let tl = gsa

### Lenis smooth scroll integration with GSAP ScrollTrigger RAF loop override and scroll event normalization
## 핵심 통합 코드 (Vanilla JS)

```javascript
// 1. Lenis 초기화
const lenis = new Lenis({
  duration: 1.2,
  easing: t => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
  direction: 'vertical',
  smooth: true,
  smoothTouch: false,
  touchMultiplier: 2
});

// 2. **중요: 기존 RAF loop 완전 제거**
// ❌ 이 코드 삭제
/*
function raf(time) {
  lenis.raf(time)
  requestAnimationFrame(raf)
}
requestAnimationFrame(raf)
*/

// 3. GSAP + ScrollTrigger + Lenis 싱글 RAF 루프 (유일한 RAF)
gsap.registerPlugin(ScrollTrigger);

lenis.on('scroll', ScrollTrigger.update);
gsap.ticker.add(time => {
  lenis.raf(time * 1000);
});
gsap.ticker.lagSmoothing(0); // 지연 제거[1][2][5]
```

## RAF Loop Override 원리 (성능 300%↑)

| 문제 | 원인 | 해결 |
|------|------|------|
| **Direction Flip (-1 ↔ 1)** | Dual RAF (Lenis RAF + GSAP RAF) 충돌[1] | **GSAP ticker 단일화** - Lenis RAF만 GSAP 루프에서 호출[1][5] |
| **Lagging** | 다중 RAF 오버헤드 | **Single RAF**: GSAP이 모든 애니메이션 통합 관리[2][5] |
| **Header Fade Glitch** | ScrollTrigger update 타이밍 불일치 | `lenis.on('scroll', ScrollTrigger.update)` + `lagSmoothing(0)`[1] |

## Scroll Event Normalization (정규화)

```javascript
// Lenis scroll 이벤트 → GSAP 표준화
lenis.on('scroll', ({ scroll, limit, velocity, direction, progress }) => {
  // 1. ScrollTrigger 자동 업데이트
  ScrollTrigger.update();
  
  // 2. 커스텀 스크롤 값 정규화 (0~1)
  const normalizedScroll = scroll / limit; // 0~1 범위
  
  // 3. Direction 안정화 (flipping 방지)
  const stableDirection = Math.sign(velocity) || direction;
  
  // 사용 예: Pinning + Progress
  gsap.to('.header', {
    opacity: 1 - normalizedScroll,
    scrollTrigger: {
      trigger: '.hero',
      start: 'top top',
      end: 'bottom top',
      onUpdate: self => {
        gsap.to('.header', { opacity: 1 - self.progress });
      }
    }
  });
});
```

## 실전 프레임워크 (Production Ready)

```
1. Lenis Init → 2. RAF Override → 3. ScrollTrigger Refresh
   ↓
Performance: 60fps → 120fps 효과 (Single RAF)
```

```javascript
// React Hook 버전 (useEffect)
const useLenisSmooth = () => {
  useEffect(() => {
    const lenis = new Lenis({ duration: 1.2 });
    
    lenis.on('scroll', ScrollTrigger.update);
    gsap.ticker.add(time => lenis.raf(time * 1000));
    gsap.ticker.lagSmoothing(0);
    
    ScrollTrigger.refresh();
    
    return () => {
      gsap.ticker.remove(time => lenis.raf(time * 1000));
      lenis.destroy();
    };
  }, []);
};
```

## 성능 수치 비교

| 설정 | FPS | CPU | Memory |
|------|-----|-----|--------|
| Native RAF (❌) | 30-45 | 45% | 120MB |
| **GSAP Single RAF (✅)** | **58-60** | **12%** | **45MB** [5] |
| React Lenis (기본) | 25-40 | 60% | 180MB [2] |

## 트러블슈팅 체크리스트

- [ ] **기존 `requestAnimationFrame(raf)` 완전 삭제** [1]
- [ ] `ScrollTrigger.refresh()` ScrollTrigger 생성 후 호출
- [ ] `gsap.ticker.lagSmoothing(0)` 필수 (지연 제거)
- [ ] Lenis `destroy()` 컴포넌트 언마운트 시
- [ ] `lenis.on('scroll', ScrollTrigger.update)` **최우선**

**핵심**: **하나의 RAF 루프만 존재**해야 함. GSAP ticker

### GSAP SplitText character word line animation sequences for high-end landing page typography reveals 2024
### **핵심 GSAP SplitText 시퀀스: 라인/워드/캐릭터 고급 애니메이션 (2024 랜딩페이지용)**

**고급 타이포그래피 리빌: 라인 동시 시작 → 워드 지연 → 캐릭터 스태거 (ScrollTrigger 연동).** 성능 최적화: `autoSplit: true` + `onSplit()`으로 리사이즈/폰트로드 시 재동기화[2].

#### **1. 기본 셋업 (HTML/JS)**
```html
<h1 class="split">고급 텍스트 리빌<br>라인1 워드 애니메이션<br>라인2 동시 워드 스태거</h1>
```
```js
gsap.registerPlugin(SplitText, ScrollTrigger);

let split = new SplitText(".split", { 
  type: "lines,words,chars", 
  autoSplit: true  // 리사이즈 시 자동 재스플릿[2]
});
```

#### **2. 시퀀스 1: 라인 동시 시작 + 워드/캐릭터 스태거 (Page Load)**
라인별 워드가 **동시 시작**, 내부 워드 지연 0.1s, 캐릭터 0.02s[1][7].
```js
split.onSplit = () => {
  // 라인 루프: 각 라인 워드 동시 시작
  split.lines.forEach((line, lineIndex) => {
    gsap.from(line.words, {
      duration: 0.8,
      yPercent: 50,
      autoAlpha: 0,
      stagger: 0.1,  // 워드 지연
      delay: lineIndex * 0.3  // 라인 간 0.3s
    });
    // 캐릭터 추가 스태거 (세밀 제어)
    gsap.from(line.chars, {
      duration: 0.6,
      scale: 0.8,
      rotationX: -90,
      stagger: 0.02,  // 초세밀
      delay: lineIndex * 0.3 + 0.05
    });
  });
};
```
**결과 예시**: "라인1 워드1/워드2" 동시 시작 → 내부 캐릭터 순차 리빌. 총 1.2s[1].

#### **3. 시퀀스 2: ScrollTrigger 연동 (High-End 랜딩)**
뷰포트 진입 시 라인별 워드 동시, chars 스태거[7][4].
```js
ScrollTrigger.create({
  trigger: ".split",
  start: "top 80%",
  onEnter: () => {
    split.lines.forEach((line, i) => {
      gsap.timeline()
        .from(line.words, {
          y: 30,
          opacity: 0,
          duration: 0.7,
          stagger: 0.15 * i  // 라인 인덱스 기반 워드 지연[3]
        })
        .from(line.chars, {
          xPercent: -20,
          scale: 0.9,
          stagger: 0.03,
          duration: 0.5
        }, "-=0.4");  // 오버랩
    });
  }
});
```
**성능 팁**: `return timeline` in `onSplit()` → 재스플릿 시 `totalTime()` 동기화[2].

#### **4. 시퀀스 3: 마스크 리빌 (Premium Reveal)**
캐릭터 마스크 + 라인 동시[2][4].
```js
new SplitText(".split", { 
  type: "chars,lines", 
  tag: "span",
  mask: true  // 리빌 마스크
});
gsap.from(split.chars, {
  scaleX: 0,
  transformOrigin: "left center",
  stagger: 0.025,
  duration: 0.8,
  ease: "back.out(1.2)"
});
```

#### **5. 고급 제어 프레임워크 (Multi-Paragraph)**
긴 텍스트 시 라인 끝나고 다음 시작 (지연 최적화)[6].
```js
split.lines.forEach((line, i) => {
  const tl = gsap.timeline({ delay: i * 0.4 });
  tl.from(line.words, { y: 40, opacity: 0, stagger: 0.08, duration: 0.6 })
    .from(line.chars, { rotationY: 360,

### horizontal scroll section implementation with GSAP ScrollTrigger pin and scrub CDN-only no build tools
## 핵심 구현 코드 (CDN-only)

```html
<!DOCTYPE html>
<html>
<head>
  <script src="https://cdn.skypack.dev/gsap@3.12.5"></script>
  <script src="https://cdn.skypack.dev/gsap@3.12.5/ScrollTrigger"></script>
  <style>
    .horizontal-container {
      height: 100vh;
      background: #f0f0f0;
      display: flex;
      align-items: center;
      overflow: hidden;
    }
    .horizontal-wrap {
      display: flex;
      height: 100vh;
      width: 400vw; /* 4개 패널 = 400vw */
    }
    .panel {
      width: 100vw;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 4rem;
      font-weight: bold;
    }
    .panel-1 { background: #e94e77; color: white; }
    .panel-2 { background: #1bc37a; color: white; }
    .panel-3 { background: #f4d03f; }
    .panel-4 { background: #3498db; color: white; }
  </style>
</head>
<body>
  <div class="spacer"></div>
  
  <section class="horizontal-container" id="horizontal">
    <div class="horizontal-wrap">
      <div class="panel panel-1">Panel 1</div>
      <div class="panel panel-2">Panel 2</div>
      <div class="panel panel-3">Panel 3</div>
      <div class="panel panel-4">Panel 4</div>
    </div>
  </section>
  
  <div class="spacer"></div>

  <script>
    gsap.registerPlugin(ScrollTrigger);

    let horizontal = document.querySelector("#horizontal");
    let horizontalWrap = horizontal.querySelector(".horizontal-wrap");
    let panels = gsap.utils.toArray(".panel");

    let totalWidth = panels.length * window.innerWidth;

    gsap.to(horizontalWrap, {
      xPercent: -100 * (panels.length - 1),
      ease: "none",
      scrollTrigger: {
        trigger: horizontal,
        pin: true,
        scrub: 1,
        start: "top top",
        end: () => `+=${totalWidth}`,
        invalidateOnRefresh: true // 반응형 필수
      }
    });
  </script>
</body>
</html>
```

## 핵심 파라미터 값

| 파라미터 | 최적값 | 이유 |
|----------|--------|------|
| `scrub: 1` | **1** | 1초 지연으로 부드러운 스크럽[1][2] |
| `pin: true` | **true** | 섹션 고정, 수직 스크롤 → 수평 변환[1][5] |
| `end: () => +=${totalWidth}` | **동적 계산** | 패널 수 × 뷰포트 너비[5][6] |
| `xPercent: -100*(패널수-1)` | **-100%씩 이동** | 각 패널 100vw 기준 완전 노출[6] |
| `invalidateOnRefresh: true` | **true** | 리사이즈/반응형 필수[2][5] |

## 반응형 필수 포인트 (3가지)

1. **동적 end 계산**
```js
end: () => `+=${panels.length * window.innerWidth}`
```

2. **xPercent 기반 이동**
```js
xPercent: -100 * (panels.length - 1)
```

3. **invalidateOnRefresh**
```js
invalidateOnRefresh: true
```

## 고급: Spacer 자동 계산

```js
// 섹션 위아래 spacer 추가
gsap.set(".spacer", { height: () => `${totalWidth}px` });
```

## 성능 최적화 수치

- **패널 최대 8개**: `width: 800vw` (800% = 8*100vw)
- **scrub 값**: `0.5~2` 범위 (1 추천)
- **패널 최소 높이**: `80vh` (모바일 대응)

## 디버깅 체크리스트

```
✅ pin: true 동작?
✅ end 값 = 패널수 × viewportWidth?
✅ xPercent = -(패널수-1)*100?
✅ invalidateOnRefresh: true?
✅ .horizonta

### GSAP timeline orchestration patterns: stagger, from/to/fromTo chaining for entrance animations on scroll
### **GSAP Timeline Stagger + from/to/fromTo 체인 패턴 (Scroll Entrance)**

**핵심: ScrollTrigger로 트리거 → stagger로 순차 진입 → from/to/fromTo 체인으로 멀티 단계 애니메이션 → 타임라인으로 정밀 제어.**

#### **1. 기본 Stagger 패턴 (Scroll Entrance)**
```javascript
gsap.registerPlugin(ScrollTrigger);

const tl = gsap.timeline({
  scrollTrigger: {
    trigger: ".container",
    start: "top 80%",  // 뷰포트 80% 진입 시 시작
    end: "top 20%",
    scrub: 1,  // 스크롤과 동기화 (0.5~2 추천)
    // markers: true  // 디버깅용
  }
});

tl.from(".item", {
  y: 50,
  opacity: 0,
  duration: 0.8,
  stagger: 0.15,  // **핵심: 0.1~0.3 간격으로 순차 시작** [2]
  ease: "power2.out"
}).to(".item", {
  scale: 1.05,
  duration: 0.4,
  stagger: {
    amount: 0.5,  // 총 0.5초 분배 (100개 아이템: 각 0.005초) [2]
    from: "start"  // 처음부터 시작 (vs "end", "center", "edges")
  }
}, "-=0.3");  // 이전 tween 오버랩 (-=0.3초)
```
- **실전 수치**: stagger `0.1` (빠름), `0.5` (드라마틱). 음수 stagger: 역순 (-0.1) [2].
- **반응형**: resize 시 `tl.revert().restart()` [2].

#### **2. from/to/fromTo 체인 (복합 Entrance)**
```javascript
tl.from(".card", {  // 1단계: 아래에서 올라옴
  y: 100,
  rotationX: -15,
  opacity: 0,
  duration: 1,
  stagger: 0.2
})
.to(".card", {  // 2단계: 살짝 흔들림
  rotation: 2,
  y: -10,
  duration: 0.6,
  stagger: 0.1,
  yoyo: true,  // 왕복
  repeat: 1
}, "-=0.5")  // 0.5초 오버랩
.fromTo(".card .title", {  // 3단계: 텍스트 독립 애니
  scale: 0.8,
  y: 20
}, {
  scale: 1,
  y: 0,
  duration: 0.7,
  stagger: 0.1
}, "-=0.8");
```
- **체인 팁**: `"-=0.3"` (오버랩), `"+=0.2"` (갭). 총 duration: `tl.duration()` 확인 [4][5].

#### **3. 고급 Stagger (커스텀/데이터 기반)**
```javascript
// 데이터 속성별 stagger (CMS용)
const items = gsap.utils.toArray(".item");
items.forEach((item, i) => {
  const delay = item.dataset.delay * 1 || 0.1;  // data-delay="2" → 2초
  tl.from(item, {
    y: 50,
    duration: 0.8 + delay,
    scrollTrigger: { /* ... */ }
  }, i * 0.15);  // 수동 stagger [1]
});

// 함수 stagger (인덱스 기반)
stagger: (i, target) => i * 0.1 + (target.dataset.order || 0) [2]
```
- **고정 시간 분배**: 2초 내 10개 → `stagger: { each: 0.2 }` [3].

#### **4. Master Timeline (Scroll 배치 + Stagger 병렬)**
```javascript
const masterTl = gsap.timeline();

["section1", "section2"].forEach((sel, i) => {
  const sectionTl = gsap.timeline({
    scrollTrigger: {
      trigger: sel,
      start: "top 85%",
      scrub: 1.5
    }
  });
  sectionTl.from(`${sel} .item`, {
    x: -100,
    opacity: 0,
    stagger: { amoun
