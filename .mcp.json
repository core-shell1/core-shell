{
  "mcpServers": {
    "mcp-kling": {
      "command": "npx",
      "args": [
        "-y",
        "mcp-kling@latest"
      ],
      "env": {
        "KLING_ACCESS_KEY": "${KLING_ACCESS_KEY}",
        "KLING_SECRET_KEY": "${KLING_SECRET_KEY}"
      }
    },
    "notebooklm": {
      "_comment": "활성화하려면 NOTEBOOKLM_EMAIL/PASSWORD .env에 추가하고 이 주석 제거. Google 계정 OAuth 필요할 수 있음.",
      "command": "npx",
      "args": [
        "-y",
        "notebooklm-mcp@latest"
      ],
      "env": {
        "NOTEBOOKLM_EMAIL": "${NOTEBOOKLM_EMAIL}",
        "NOTEBOOKLM_PASSWORD": "${NOTEBOOKLM_PASSWORD}"
      }
    }
  },
  "_disabled_fal_ai": {
    "reason": "fal-mcp-server uvx 연결 불안정. 대신 company/tools/nano_banana.py 사용",
    "restore": "이 블록 복원 + mcpServers 안으로 이동"
  }
}